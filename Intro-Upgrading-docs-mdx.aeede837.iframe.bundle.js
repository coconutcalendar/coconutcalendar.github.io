(self.webpackChunk_coconut_software_ui=self.webpackChunk_coconut_software_ui||[]).push([[4625],{"./src/Intro/Upgrading.docs.mdx":(__unused_webpack_module,__webpack_exports__,__webpack_require__)=>{"use strict";__webpack_require__.r(__webpack_exports__),__webpack_require__.d(__webpack_exports__,{default:()=>MDXContent});__webpack_require__("./node_modules/react/index.js");var jsx_runtime=__webpack_require__("./node_modules/react/jsx-runtime.js"),lib=__webpack_require__("./node_modules/@storybook/addon-docs/node_modules/@mdx-js/react/lib/index.js"),blocks=__webpack_require__("./node_modules/@storybook/addon-docs/dist/blocks.mjs");const UPGRADE_namespaceObject="# Upgrade Guide\n\n## From v0.18 to v0.19\n\n### Breaking Changes\n\nThis release introduces several breaking changes:\n\n#### 1. Tailwind CSS Upgrade (v1.9 → v3.4.10)\n\nThis library has been upgraded from Tailwind CSS v1.9 to v3.4.10. This is a **major version upgrade** that introduces significant breaking changes that will affect your application. As tailwind is listed as a peer dependency by Breeze, you must also update your tailwind CSS implementation.\n\n**Required Actions:**\n\n1. **Upgrade Tailwind CSS in your project:**\n\n   ```bash\n   npm install tailwindcss@^3.4.10\n   # or\n   yarn add tailwindcss@^3.4.10\n   ```\n\n2. **Update your `tailwind.config.js` file:**\n\n   Tailwind v3 requires several configuration changes:\n\n   **a. Replace `purge` with `content`:**\n\n   ```js\n   // Before (v1.9)\n   module.exports = {\n     purge: ['./src/**/*.{js,jsx,ts,tsx}'],\n     // ...\n   }\n\n   // After (v3.4.10)\n   module.exports = {\n     content: ['./src/**/*.{js,jsx,ts,tsx}'],\n     // ...\n   }\n   ```\n\n   **b. Remove `future` configuration flags:**\n\n   ```js\n   // Before (v1.9)\n   module.exports = {\n     future: {\n       removeDeprecatedGapUtilities: true,\n       purgeLayersByDefault: true,\n     },\n     // ...\n   }\n\n   // After (v3.4.10)\n   // Remove the entire 'future' block - these are now default behaviors\n   module.exports = {\n     // ...\n   }\n   ```\n\n   **c. Update content paths to include library components:**\n\n   ```js\n   module.exports = {\n     content: [\n       './src/**/*.{js,jsx,ts,tsx}',\n       './node_modules/@coconut-software/ui/**/*.js', // Add this line\n     ],\n     // ...\n   }\n   ```\n\n3. **Merge library configuration with your Tailwind config:**\n\n   If you're using the library's default theme configuration, merge it properly:\n\n   ```js\n   const defaultConfig = require('@coconut-software/ui/defaultConfig')\n\n   module.exports = {\n     content: [\n       './src/**/*.{js,jsx,ts,tsx}',\n       './node_modules/@coconut-software/ui/**/*.js',\n     ],\n     theme: {\n       extend: {\n         ...defaultConfig.theme.extend,\n         // Your custom theme extensions here\n       },\n     },\n     plugins: [\n       require('tailwindcss-interaction-variants'),\n       // Your other plugins\n     ],\n   }\n   ```\n\n4. **Update PostCSS configuration (if needed):**\n\n   Ensure your PostCSS config includes Tailwind and Autoprefixer:\n\n   ```js\n   // postcss.config.js\n   module.exports = {\n     plugins: {\n       tailwindcss: {},\n       autoprefixer: {},\n     },\n   }\n   ```\n\n**Key Breaking Changes:**\n\n- **JIT Mode**: Tailwind v3 uses Just-In-Time (JIT) compilation by default. This means only classes you actually use will be generated. Make sure your `content` paths are correctly configured.\n\n- **Utility Class Changes**: Some utility classes have been renamed or deprecated:\n  - Color utilities may have changed (check Tailwind v3 migration guide)\n  - Some responsive variants behave differently\n\n- **Plugin Compatibility**: Ensure all Tailwind plugins you use are compatible with v3.4.10. The library requires `tailwindcss-interaction-variants@^5.0.0`.\n\n- **CSS Import Changes**: If you're importing Tailwind directly, ensure you're using the v3 syntax:\n\n   ```css\n   @import \"tailwindcss/base\";\n   @import \"tailwindcss/components\";\n   @import \"tailwindcss/utilities\";\n   ```\n\n**Migration Resources:**\n\nFor detailed migration information, refer to the [official Tailwind CSS v3 upgrade guide](https://tailwindcss.com/docs/upgrade-guide) for v2->v3 and [official Tailwind CSS v2 upgrade guide](https://v2.tailwindcss.com/docs/upgrading-to-v2) for v1->v2.\n\n#### 2. Focus Indicator Colors\n\nRemoved support for `ring-primary` and `ring-secondary` focus indicator colors.\n\n**Action Required:**\n- Use supported `ring` and `ring-inset` instead which offer black/white contrast\n- Remove any custom focus ring utilities that rely on these\n- Use standard Tailwind focus ring utilities or define custom focus styles\n\n#### 3. Dialog Component Changes\n\n**Breaking Changes:**\n- The `dismissable` prop now defaults to `true` (previously `false`)\n- When `dismissable` is `false`, the `onDismiss` callback will not be called on `Esc` or backdrop click\n- The close icon is not visible when `dismissable` is `false`\n\n**Action Required:**\n\n```jsx\n// If you were relying on dismissable defaulting to false:\n<Dialog dismissable={false} onDismiss={handleDismiss}>\n  {/* content */}\n</Dialog>\n\n// This now requires explicit handling if you want to prevent dismissal\n// The behavior is now: dismissable={false} means truly non-dismissable\n```\n\n#### 4. Menu.Item Component\n\nThe `danger` prop has been renamed to `error` for consistency with other components.\n\n**Action Required:**\n\n```jsx\n// Before\n<Menu.Item danger onClick={handleDelete}>\n  Delete\n</Menu.Item>\n\n// After\n<Menu.Item error onClick={handleDelete}>\n  Delete\n</Menu.Item>\n```\n\n#### 5. Error Color Changes\n\nError colors have been updated to meet WCAG 2.2 color contrast standards. If you have custom components or styles using error colors, they may appear different.\n\n**Action Required:**\n- Review any custom components using error colors\n- Test color contrast in your application\n- Update custom error color styles if needed\n\n### New Features\n\n- **Documentation**: Added documentation pages for components that underwent changes in this release.\n- **Storybook**: Updated storybook formatting for several components to use the new story format. Refer to the [Component Story Format (CSF) documentation](https://storybook.js.org/docs/api/csf) for more details.\n- **Tag Component**: Introduced a new `Tag` component for conveying short metadata about an item.\n\n  ```jsx\n  <Tag\n   color=\"blue\"\n   label=\"Offline\"\n   size=\"small\"\n   startAdornment={Icons.Star}\n  />\n  ```\n\n- **TextInput Component**: Added a new input type `type=\"color\"` to the `<TextInput>` component.\n\n  ```jsx\n  <TextInput\n   helperText=\"This is a color picker\"\n   label=\"Color\"\n   type=\"color\"\n  />\n  ```\n\n  For more about the color type input, refer to [MDN documentation](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/color).\n\n- **New Icons**: Introduced multiple new icons including:\n  - Expand/Collapse icons: `PanelOpen`, `PanelClose`\n  - Volume control icons: `VolumeDown`, `VolumeMute`, `VolumeUp`\n  - Meeting files icons: `DownloadChat`, `DownloadCoBrowsing`, `DownloadRecording`, `DownloadTranscript`, `MeetingFile`\n  - Layout icons: `Columns`, `Rows`\n  - Dashboard icons: `Calendar`, `Home`\n  - Utility icons: `HourglassEmpty`, `Lunch`, `Support`, `TurnRight`, `Walk`, `Map`, `Tune` \n\n### Updates\n\n- **Tailwind**: Updated Tailwind from v1.9 to v3.4.10.\n- **Avatar**: Adjusted avatar colors to meet WCAG 2.2 color contrast standards.\n- **Chip**: Adjusted default background color to `bg-black-5` and added optional `helperText` support.\n- **Checkbox**: Added optional `helperText` support.\n- **Dialog**: Background scrolling is disabled when the `<Dialog>` component is open.\n- **List.Item**: Added support for `defaultOpen` prop and `onClick` callback.\n- **Menu.Item**: Updated styles for selected and hovered menu items.\n- **Tooltip**:\n  - Added support for dismissing tooltip with 'esc' key even when hovered.\n  - Long text within the `<Tooltip>` component now wraps.\n- **Focus Indicator**: Added universal focus indicator to `<Button>` and `<Link>` components.\n- **Error Colors**: Added `error` color option to `<Button>` and `<Checkbox>` elements.\n- **Colors Categorization**: Introduced `colors.ts` file categorizing colors as 'Functional', 'Brand', or 'Utility'. 'Functional' colours are intended to convey meaning of status such as 'error' or 'warning'. 'Brand' colours are those defined by you through CSS variables as either 'primary' or 'secondary'. 'Utility' colours are colours used to differentiate items with no inherent meaning. These are often used in interfaces to support easy scanning through pops of colour, as well as relate certain types of items as related. Examples of components using Utility colours are the `<Avatar>` and `<Tag>` components.\n\n## From v0.17 to v0.18\n\nThis release changes how nested Lists are structured. This affects the following components\n* List\n* List/Item\n\nThe new structure makes use of composition rather than passing a nested list\nvia props. As well, the collapsed state is now contained inside the List \ncomponent and there is no longer a need for a Collapse component to be used.\n\n```jsx\n// Before\n<List>\n  <List.Item/>\n\n  <Collapse open={state.open}>\n    <List nested={1}>\n      <List.Item/>\n      <List.Item/>\n    </List>\n  </Collapse>\n</List>\n```\n\n```jsx\n// After\n<List>\n  <List.Item>\n    <List nested={1}>\n      <List.Item />\n      <List.Item />\n      <List.Item />\n    </List>\n  </List.Item>\n</List>y\n```\n\nThis release also introduces many accessibility changes such as\n* no more nested interactive elements\n* element hierarchy within lists is now correct. `ul` > `li` is respected always.\n* add checkmark to selectable list items\n\n## From v0.16 to v0.17\n\nThis release drops support for React v16. If you are running anything older than v17.0, you will need to upgrade. Luckily, it is a painless process since it was [a non-breaking release](https://legacy.reactjs.org/blog/2020/10/20/react-v17.html).\n\n## From v0.15 to v0.16\n\nThis release changes the way any dropdowns are hidden from the DOM when closed. Previously, they were only visually hidden but still accessible to assistive technology. In this version, closed dialogs and dropdowns are hidden from assistive technology with `display: none` style. The change affects the following components:\n1. Autocomplete\n2. DatePickerInput\n3. Menu\n4. MultiSelect\n5. Select\n6. Table (with header dropdowns and row actions).\n\n**Note:** `<Dialog>` component was changed to follow the same pattern as the dropdowns. However, it fully removes its contents from the DOM when it's closed, so it's not affected by the changes.\n\nWhile there is no change in functionality for the components mentioned above, any tests relying on visually hidden elements to still be accessible will break. To fix them, make sure to wait for the elements to become visible. For example:\n\n```jsx\n// Before: invisible options are accessible; menus are \"visible\" in tests even when they are visually hidden on the page\nconst autocomplete = screen.getByRole('combobox', { name: /Autocomplete/i })\nconst menu = screen.getByRole('listbox', { name: /Autocomplete/i })\nconst option = within(menu).getByRole('option', { name: /Option One/i })\n\nexpect(menu).toBeVisible()\nexpect(menu).toHaveClass('hidden')\n\nuserEvent.click(option)\n\n// After: only visible element are accessible; menu visibility in tests works as expected\nconst autocomplete = screen.getByRole('combobox', { name: /Autocomplete/i })\n\nuserEvent.click(autocomplete)\n\nconst menu = screen.getByRole('listbox', { name: /Autocomplete/i })\nconst option = within(menu).getByRole('option', { name: /Option One/i })\n\nexpect(menu).toBeVisible()\nexpect(option).toBeVisible()\n\nuserEvent.click(option)\n```\n\n## From v0.14 to v0.15\n\nIf you are utilizing `Progress` component with a message, you will have to pass the message with `Progress.Description`. Previously, the message always had the ARIA role of `alert`, which often was too agressive, as alerts interrupt the announcement of a user's current activity. Similarly, a `Badge` component used to always have a role of `status`. This release allows setting the following roles on `Badge` and `Progress` description: `alert`, `status` and `presentation`. By default, those components do not have an ARIA role, so their content will not be announced by assistive technology.\n\n```jsx\n// Before: the message is always an alert\n<Progress message=\"Signing in to Coconut\" variant=\"circular\" />\n\n// After: the message role can be set to status\n<Progress variant=\"circular\">\n  <Progress.Description role={AriaLiveRole.Status}>\n    Signing in to Coconut\n  </Progress.Description>\n</Progress>\n```\n\nThis release also allows providing `aria-label` to `Badge` and `aria-labelledby`, `aria-label`, `aria-valuetext` and `id` to `Progress` component. When computing an accessible name for the progress, \n`aria-labelledby` will have the highest priority, followed by `aria-label`. If none of them are provided, the description will be used as an accessible name. An accessible name is required for a progressbar. `aria-valuetext` allows giving a more understandable value description when the value is not a percentage. For example:\n\n```jsx\n// The value will be read as \"2 out of 10\" instead of \"20\"\nconst value = 20\nconst max = 10\n\nreturn (\n  <Progress\n    aria-valuetext={`${Math.floor(\n      (value / 100) * max,\n    )} out of ${max}`}\n    color=\"secondary\"\n    value={value}\n    variant=\"circular\"\n  />\n)\n```\n\nThis release also ensures that the `Autocomplete` and other text input components pass color contrast accessibility requirements as specified by WCAG AA. As part of this change, `error` colors have been updated to be more consistent with [new Tailwind colors](https://tailwindcolor.com/). Note that any Design System or custom componets using `error` colors will be affected.\n\n![Error colors](/assets/images/error_colors.png)\n\n## From v0.13 to v0.14\n\nThis release introduces a potentially breaking change to do with the `Dialog` component. During a debugging session it was discovered that the `FocusLock` component from [react-focus-lock](https://github.com/theKashey/react-focus-lock) package was the cause of major slowdowns in external application testing suites. Be sure to check out your test suite when you upgrade for any failures. If you need to opt into having that component not be disabled for any reason (you're checking specific focus inside of a `Dialog` for example), you can dynamically add the `ENABLE_FOCUS_LOCK` environment variable with a set of `jest` globals.\n\n```js\nafterAll(() => {\n  delete process.env.ENABLE_FOCUS_LOCK\n})\n\nbeforeEach(() => {\n  process.env.ENABLE_FOCUS_LOCK = 'true'\n})\n```\n\n## From v0.12 to v0.13\n\nIn order to provide a better developer experience with regards to creating forms with [React Hook Form](https://react-hook-form.com/) that require the ability to programmatically change input values and such, we've released some opt-in changes in the form of a new custom hook that will allow you to set a value on a given input by name and have the appropriate events propogate correctly.\n\n```jsx\n// Before\nfunction App() {\n  const options = [\n    { label: 'One', value: '1' },\n    { label: 'Two', value: '2' },\n  ]\n\n  const textRef = useRef(null)\n  const dateRef = useRef(null)\n  const selectRef = useRef(null)\n  const radiosRef = useRef({})\n  const checkboxOneRef = useRef(null)\n  const checkboxTwoRef = useRef(null)\n\n  const changeInput = (input, value) => {\n    if (input) {\n      const descriptor = Object.getOwnPropertyDescriptor(\n        window.HTMLInputElement.prototype,\n        'value',\n      )\n      const inputEvent = new Event('input', { bubbles: true })\n      descriptor.set?.call(input, value)\n      input.dispatchEvent(inputEvent)\n    }\n  }\n\n  const clickSelectOption = (input, label) => {\n    if (input) {\n      const listbox = document.getElementById(\n        (input.nextSibling.firstChild.firstChild.firstChild).getAttribute('aria-controls'),\n      )\n      const option = listbox?.querySelector(\n        `li > button[title=\"${label.toLowerCase()}\"]`,\n      )\n      option?.click()\n    }\n  }\n\n  const clickRadioCheckbox = (input, value) => {\n    if (input) {\n      const descriptor = Object.getOwnPropertyDescriptor(\n        window.HTMLInputElement.prototype,\n        'checked',\n      )\n      const inputEvent = new Event('click', { bubbles: true })\n      descriptor.set?.call(input, value)\n      input.dispatchEvent(inputEvent)\n    }\n  }\n\n  const registerRadio = (element, value) => {\n    radiosRef.current[value] = element\n  }\n\n  useEffect(() => {\n    changeInput(textRef.current, 'Some text')\n    // Changing the input value does not have any effect\n    // on the date selected in date picker\n    changeInput(dateRef.current, 'Jan 1, 2021')\n    // We have to wait to for select options to have title set\n    setTimeout(() => {\n      clickSelectOption(selectRef.current, options[1].label)\n    }, 1)\n    clickRadioCheckbox(radiosRef.current[1], true)\n    clickRadioCheckbox(checkboxOneRef.current, true)\n    clickRadioCheckbox(checkboxTwoRef.current, true)\n  }, [])\n\n  return (\n    <form>\n      <TextInput inputRef={textRef} label=\"Text Input\" name=\"textinput\" />\n      <DayPickerInput inputRef={dateRef} label=\"Date\" name=\"date\" />\n      <Select inputRef={selectRef} label=\"Select\" name=\"select\">\n        {options.map(({ label, value }) => (\n          <Select.Option key={value} value={value}>\n            {label}\n          </Select.Option>\n        ))}\n      </Select>\n      <Radio.Group name=\"radio\">\n        <Radio.Label>Radio</Radio.Label>\n        {options.map(({ label, value }) => (\n          <Radio.Input\n            inputRef={(element) => registerRadio(element, value)}\n            key={value}\n            value={value}\n          >\n            {label}\n          </Radio.Input>\n        ))}\n      </Radio.Group>\n      <Checkbox.Group>\n        <Checkbox.Label>Checkbox</Checkbox.Label>\n        <Checkbox.Input inputRef={checkboxOneRef} name=\"checkbox-one\">\n          First Option\n        </Checkbox.Input>\n        <Checkbox.Input inputRef={checkboxTwoRef} name=\"checkbox-two\">\n          Second Option\n        </Checkbox.Input>\n      </Checkbox.Group>\n    </form>\n  )\n}\n```\n\n```jsx\n// After\nfunction App() {\n  const { register, setValue } = useForm()\n\n  const options = [\n    { label: 'One', value: '1' },\n    { label: 'Two', value: '2' },\n  ]\n\n  useEffect(() => {\n    setValue('textinput', 'Some text')\n    setValue('date', 'Jan 1, 2021')\n    setValue('select', options[1].value)\n    setValue('radio', options[1].value)\n    setValue('checkbox-one', true)\n    setValue('checkbox-two', true)\n  }, [])\n\n  return (\n    <form>\n      <TextInput inputRef={register} label=\"Text Input\" name=\"textinput\" />\n      <DayPickerInput inputRef={register} label=\"Date\" name=\"date\" />\n      <Select inputRef={register} label=\"Select\" name=\"select\">\n        {options.map(({ label, value }) => (\n          <Select.Option key={value} value={value}>\n            {label}\n          </Select.Option>\n        ))}\n      </Select>\n      <Radio.Group name=\"radio\">\n        <Radio.Label>Radio</Radio.Label>\n        {options.map(({ label, value }) => (\n          <Radio.Input inputRef={register} key={value} value={value}>\n            {label}\n          </Radio.Input>\n        ))}\n      </Radio.Group>\n      <Checkbox.Group>\n        <Checkbox.Label>Checkbox</Checkbox.Label>\n        <Checkbox.Input inputRef={register} name=\"checkbox-one\">\n          First Option\n        </Checkbox.Input>\n        <Checkbox.Input inputRef={register} name=\"checkbox-two\">\n          Second Option\n        </Checkbox.Input>\n      </Checkbox.Group>\n    </form>\n  )\n}\n```\n\n## From v0.11 to v0.12\n\nIn order to provide a better purging experience and reduce random cascading issues, we have changed the default style export so that it no longer includes the base Tailwind CSS classes, but instead only the necessary custom CSS that you will need to include in your own project.\n\nThis guide is going to assume that you are importing the default stylesheet into your main CSS file already, in a manner such as this and have Tailwind CSS set up according to their guidelines.\n\n```css\n@import \"~@coconut-software/ui\";\n```\n\nIn order to maintain proper purging of unused CSS in production, you will need to update the `purge` configuration within your `tailwind.config.js` file to include the following.\n\n```js\nmodule.exports = {\n  ...\n  purge: [\n    './node_modules/@coconut-software/ui/**/*.js',\n    ...\n  ],\n  ...\n}\n```\n\nYou will also need to update your `variants` configuration within your `tailwind.config.js` file to include the default variants used by the design system.\n\n```js\nconst defaultVariants = require('@coconut-software/ui/defaultVariants')\n\nmodule.exports = {\n  ...\n  variants: {\n    ...defaultVariants,\n  },\n  ...\n}\n```\n\nLastly, you will need to install `tailwindcss-interaction-variants` and add it to the `plugins` section of your `tailwind.config.js` file.\n\n```js\nmodule.exports = {\n  ...\n  plugins: [require('tailwindcss-interaction-variants')],\n  ...\n}\n```\n\n## From v0.10 to v0.11\n\nIf you are utilizing the `Table.Pagination` component, you will need to update your usages to pass the total number of records from `Table.Pagination` to `Table`. The `total` prop is optional, but if you are using `Table.Pagination` it is strongly advised to keep it in sync.\n\n```jsx\n// Before\nfunction App() {\n  const columns = [{ key: 'name', selected: true, value: 'Name' }]\n  const rows = [\n    { id: '1', name: 'John Doe' },\n    { id: '2', name: 'Jane Doe' },\n    { id: '3', name: 'Jeff Doe' },\n  ]\n\n  const handleChange = ({ limit, page }) => {\n    // ...\n  }\n\n  return (\n    <Table columns={columns} rows={rows}>\n      <Table.Toolbar>\n        <Table.Pagination onChange={handleChange} total={3}>\n      </Table.Toolbar>\n    </Table>\n  )\n}\n```\n\n```jsx\n// After\nfunction App() {\n  const columns = [{ key: 'name', selected: true, value: 'Name' }]\n  const rows = [\n    { id: '1', name: 'John Doe' },\n    { id: '2', name: 'Jane Doe' },\n    { id: '3', name: 'Jeff Doe' },\n  ]\n\n  const handleChange = ({ limit, page }) => {\n    // ...\n  }\n\n  return (\n    <Table columns={columns} rows={rows} total={3}>\n      <Table.Toolbar>\n        <Table.Pagination onChange={handleChange}>\n      </Table.Toolbar>\n    </Table>\n  )\n}\n```\n\nIf you are utilizing the `inverted` prop on the `Progress` component, you must update your usages to use the `inverse` prop instead. This change is to bring the component more inline with the rest of the framework to promote better consistency and provide better cohesion.\n\n```jsx\n// Before\nfunction App() {\n  return <Progress inverted />\n}\n```\n\n```jsx\n// After\nfunction App() {\n  return <Progress inverse />\n}\n```\n\nIf you are utilizing the `inverted` color value on the `Typography` component, you must update your usages to use the `inverse` color value instead. This change is to bring the component more inline with the rest of the framework to promote better consistency and provide better cohesion.\n\n```jsx\n// Before\nfunction App() {\n  return <Typography color=\"inverted\" />\n}\n```\n\n```jsx\n// After\nfunction App() {\n  return <Typography color=\"inverse\" />\n}\n```\n\n## From v0.9 to v0.10\n\nThere are no breaking changes in this release, but a major clean up of the internal workings of the `Autocomplete`, `Menu`, `Select`, and `TextInput` components that were inappropriately using state to track things that we can do in CSS. This is a minor upgrade to ensure a more thorough check through any usages of these components for potential problems.\n\n## From v0.8 to v0.9\n\nIf you are utilizing the `Table` component, you will need to update your usages to pass actions that can be taken on a row instead of a function to call when clicking on a row as that behaviour has been removed.\n\n```jsx\n// Before\nfunction App() {\n  const columns = [{ key: 'name', selected: true, value: 'Name' }]\n  const rows = [\n    { id: '1', name: 'John Doe' },\n    { id: '2', name: 'Jane Doe' },\n    { id: '3', name: 'Jeff Doe' },\n  ]\n\n  const onClickRow = (event) => {\n    // ...\n  }\n\n  return (\n    <Table columns={columns} onClickRow={onClickRow} rows={rows} />\n  )\n}\n```\n\n```jsx\n// After\nfunction App() {\n  const columns = [{ key: 'name', selected: true, value: 'Name' }]\n  const rows = [\n    { id: '1', name: 'John Doe' },\n    { id: '2', name: 'Jane Doe' },\n    { id: '3', name: 'Jeff Doe' },\n  ]\n\n  const onDelete = () => {\n    // ...\n  }\n\n  const onEdit = () => {\n    // ...\n  }\n\n  const rowActions = [\n    { label: 'Edit', handler: onEdit },\n    { label: 'Delete', handler: onDelete },\n  ]\n\n  return (\n    <Table columns={columns} rowActions={rowActions} rows={rows} />\n  )\n}\n```\n\nThis will ultimately provide more flexibility as you are able to supply multiple actions and it solves a UX issue involving clicking on a row to trigger a callback.\n\n## From v0.7 to v0.8\n\nIf you are utilizing the `Card.Header` component, you will need to update your usages as they have been brought more inline with the rest of the framework to promote better consistency and provide better cohesion.\n\n```jsx\n// Before\nfunction App() {\n  return (\n    <Card>\n      <Card.Header\n        action={\n          <IconButton aria-label=\"Settings\" size=\"medium\">\n            <MoreVert />\n          </IconButton>\n        }\n        subtitle=\"September 14, 2016\"\n        title=\"Shrimp and Chorizo Paella\"\n      />\n      {/* ... */}\n    </Card>\n  )\n}\n```\n\n```jsx\n// After\nfunction App() {\n  return (\n    <Card>\n      <Card.Header>\n        <Card.Action>\n          <IconButton aria-label=\"Settings\" size=\"medium\">\n            <MoreVert />\n          </IconButton>\n        </Card.Action>\n        <Card.Subtitle>September 14, 2016</Card.Subtitle>\n        <Card.Title>Shrimp and Chorizo Paella</Card.Title>\n      </Card.Header>\n    </Card>\n  )\n}\n```\n\n## From v0.6 to v0.7\n\nIf you are utilizing the `Checkbox` or `Radio` components, you will need to update your usages as they have been brought more inline with not only each other, but the rest of the form components to provide more cohesion.\n\n```jsx\n// Before\nfunction App() {\n  return (\n    <>\n      <Checkbox color=\"secondary\" size=\"small\">\n        Label\n      </Checkbox>\n      <Radio.Group color=\"primary\" defaultValue={1} size=\"small\">\n        <Radio.Label>Label</Radio.Label>\n        <Radio.Button value={1}>\n        <Radio.Button value={2}>\n        <Radio.Button value={3}>\n      </Radio.Group>\n    </>\n  )\n}\n```\n\n```jsx\n// After\nfunction App() {\n  return (\n    <>\n      <Checkbox.Input color=\"secondary\" size=\"small\">\n        Label\n      </Checkbox.Input>\n      <Radio.Group color=\"primary\" defaultValue={1} size=\"small\">\n        <Radio.Label>Label</Radio.Label>\n        <Radio.Input value={1}>\n        <Radio.Input value={2}>\n        <Radio.Input value={3}>\n      </Radio.Group>\n    </>\n  )\n}\n```\n\nIn addition to renaming components, we've also introduced a `Checkbox.Group` concept similar to the `Radio.Group` component set to allow for greater flexbility and cover more form use cases.\n\n```jsx\nfunction App() {\n  return (\n    <Checkbox.Group direction=\"row\">\n      <Checkbox.Label>Label:</Checkbox.Label>\n      <Checkbox.Input color=\"primary\" size=\"small\">\n        Option 1\n      </Checkbox.Input>\n      <Checkbox.Input color=\"primary\" size=\"small\">\n        Option 2\n      </Checkbox.Input>\n      <Checkbox.Input color=\"primary\" size=\"small\">\n        Option 3\n      </Checkbox.Input>\n    </Checkbox.Group>\n  )\n}\n```\n\nLastly, we were previously returning an arbitrary string of either `checked` or `unchecked` when a checkbox was changed. We are now returning the underlying `ChangeEvent` to bring further consistency to all form components.\n\n## From v0.6.0 to v0.6.1\n\nWe've re-introduced the actions object on the `onChange` handler of the `Select` component temporarily. After testing in a live build, using just the `ref` props on the `Select.Option` components turned out to be less than optimal, as you would have to trigger a programmatic click which would be followed up by a change event.\n\n_This API may change in the future, but for now it will stick around._\n\n## From v0.5 to v0.6\n\nIf you were previously relying on the actions object passed through the `onChange` handler of the `Select` component, that has been removed in favour of adding `ref` props to each `Select.Option` component specifically. The use case for providing the actions object was to allow for reverting the selection without triggering another event.\n\n> Note: This functionality was quickly re-introduced in `v0.6.1`. See above for further details.\n\n## From v0.4 to v0.5\n\nIf you are utilizing the `Dialog` component, you will need to update your usage of the `Dialog.Header` subcomponent. Previously, you would pass a `title` prop to use as the heading of the dialog, it has now been updated to accept it as a part of the component's children in order to more closely align with the rest of the components in the package.\n\n```jsx\n// Before\nfunction App() {\n  return (\n    <Dialog>\n      <Dialog.Header title=\"Hello, World!\" />\n    </Dialog>\n  )\n}\n```\n\n```jsx\n// After\nfunction App() {\n  return (\n    <Dialog>\n      <Dialog.Header>\n        Hello, World!\n      </Dialog.Header>\n    </Dialog>\n  )\n}\n```\n\n## From v0.3 to v0.4\n\nIn order to prevent errors when using components that utilize translations, you will need to merge said translations with your own at run time since we cannot have more than one instance of the `IntlProvider` component in the component tree.\n\n```jsx\nimport { en } from '@coconut-software/ui/dist/locales'\nimport React from 'react'\nimport { IntlProvider } from 'react-intl'\n\nfunction App({ children }) {\n  const current = 'en'\n\n  return (\n    <IntlProvider\n      locale={current}\n      messages={{ messages[current], ...{ en }[current] }}\n    >\n      {children}\n    </IntlProvider>\n  )\n}\n```\n\n## From v0.2 to v0.3\n\nWe have introduced support for localizing our components in an accessible manner, so with that, you must install `react-intl` with a version constraint of `^v5.13` or higher.\n";function _createMdxContent(props){return(0,jsx_runtime.jsxs)(jsx_runtime.Fragment,{children:[(0,jsx_runtime.jsx)(blocks.W8,{title:"Intro/Upgrade Guide"}),"\n",(0,jsx_runtime.jsx)(blocks.oz,{children:UPGRADE_namespaceObject})]})}function MDXContent(props={}){const{wrapper:MDXLayout}={...(0,lib.R)(),...props.components};return MDXLayout?(0,jsx_runtime.jsx)(MDXLayout,{...props,children:(0,jsx_runtime.jsx)(_createMdxContent,{...props})}):_createMdxContent()}},"./node_modules/@storybook/addon-docs/dist sync recursive":module=>{function webpackEmptyContext(req){var e=new Error("Cannot find module '"+req+"'");throw e.code="MODULE_NOT_FOUND",e}webpackEmptyContext.keys=()=>[],webpackEmptyContext.resolve=webpackEmptyContext,webpackEmptyContext.id="./node_modules/@storybook/addon-docs/dist sync recursive",module.exports=webpackEmptyContext},"./node_modules/@storybook/addon-docs/node_modules/@mdx-js/react/lib/index.js":(__unused_webpack_module,__webpack_exports__,__webpack_require__)=>{"use strict";__webpack_require__.d(__webpack_exports__,{R:()=>useMDXComponents,x:()=>MDXProvider});var react__WEBPACK_IMPORTED_MODULE_0__=__webpack_require__("./node_modules/react/index.js");const emptyComponents={},MDXContext=react__WEBPACK_IMPORTED_MODULE_0__.createContext(emptyComponents);function useMDXComponents(components){const contextComponents=react__WEBPACK_IMPORTED_MODULE_0__.useContext(MDXContext);return react__WEBPACK_IMPORTED_MODULE_0__.useMemo((function(){return"function"==typeof components?components(contextComponents):{...contextComponents,...components}}),[contextComponents,components])}function MDXProvider(properties){let allComponents;return allComponents=properties.disableParentContext?"function"==typeof properties.components?properties.components(emptyComponents):properties.components||emptyComponents:useMDXComponents(properties.components),react__WEBPACK_IMPORTED_MODULE_0__.createElement(MDXContext.Provider,{value:allComponents},properties.children)}},"./node_modules/storybook/dist/components sync recursive":module=>{function webpackEmptyContext(req){var e=new Error("Cannot find module '"+req+"'");throw e.code="MODULE_NOT_FOUND",e}webpackEmptyContext.keys=()=>[],webpackEmptyContext.resolve=webpackEmptyContext,webpackEmptyContext.id="./node_modules/storybook/dist/components sync recursive",module.exports=webpackEmptyContext},"./node_modules/storybook/dist/theming sync recursive":module=>{function webpackEmptyContext(req){var e=new Error("Cannot find module '"+req+"'");throw e.code="MODULE_NOT_FOUND",e}webpackEmptyContext.keys=()=>[],webpackEmptyContext.resolve=webpackEmptyContext,webpackEmptyContext.id="./node_modules/storybook/dist/theming sync recursive",module.exports=webpackEmptyContext}}]);